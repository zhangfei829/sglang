---
description: SPDK/UMBP 跑 sglang 前的运行时环境检查与恢复（重启/容器重启后必做）
alwaysApply: true
---

# SPDK/UMBP 运行时环境设置（skyriver07 等多核机）

跑 `UMBP_SSD_BACKEND=spdk_proxy` 的 sglang bench 前，**先确认下列运行时状态**。
这些状态**重启 / 容器重启后全部丢失**，每次都要重做——不要等报错了才查。

## 持久（编译产物，重启不丢，无需重做）

- `/home/fizhang/spdk`：已 `--with-shared --max-lcores=512 --without-nvme-cuse` 编好（含
  `MAX_CPU_CORES 128→512` 改动，app.c:44）。
- mori main（带 UMBP+SPDK，spdk_proxy 在 `/opt/venv/lib/python3.10/site-packages/mori/spdk_proxy`）。
- sglang 分支 `test/umbp-io-bandwidth-stats-tools`、模型 `/mnt/nvme1/data/DeepSeekV3.1`。
- `/etc/ld.so.conf.d/zzz-spdk.conf`（但重启后要重跑 `ldconfig` 刷缓存）。

## skyriver07 模型路径（不要再用 /nfs/data，它重启后会消失）

`/nfs/data/DeepSeek-V3` 是早期 bind mount/软链，**重启后不会自动重建**，跑测会报
`OSError: Can't load the configuration of '/nfs/data/DeepSeek-V3'`。模型实体在 `/mnt/nvme1/data/`：

| 路径 | 说明 |
|---|---|
| `/mnt/nvme1/data/DeepSeekV3.1` | DeepSeek-V3.1，和 V3 架构兼容，**默认用这个** |
| `/mnt/nvme1/data/deepseek-r1-FP8-Dynamic-from-BF16` | R1 FP8，备用 |

跑测一律显式传 `MODEL_PATH=/mnt/nvme1/data/DeepSeekV3.1`，别依赖 runner 里 `/nfs/data` 默认值。
skyriver07 数据盘：`/mnt/raid0`(14T, md0)、`/mnt/nvme0`(1.6T)、`/mnt/nvme1`(5.9T, 模型在这)。

## 易丢（重启/容器重启后必做，按此恢复）

| 项 | 检查 | 恢复 |
|---|---|---|
| hugepage 分配 | `grep . /sys/devices/system/node/node*/hugepages/hugepages-2048kB/nr_hugepages` | `echo 24576 > .../node0/.../nr_hugepages; echo 24576 > .../node1/.../nr_hugepages`（48GB/node，proxy ring 32GB+余量） |
| hugetlbfs 挂载 | `mount \| grep -i huge` | `mkdir -p /dev/hugepages; mount -t hugetlbfs nodev /dev/hugepages` |
| vfio-pci 绑定 | `basename $(readlink /sys/bus/pci/devices/0000:88:00.0/driver)` | host: unbind nvme → `driver_override=vfio-pci` → bind（88/c3） |
| /dev/vfio 节点 | `ls -l /dev/vfio/`（要有 group 110/141） | 取 `cat /sys/class/vfio/110/dev`(=239:0) `/141`(=239:1)，容器内 `mknod -m666 /dev/vfio/110 c 239 0; mknod -m666 /dev/vfio/141 c 239 1` |
| ldconfig 缓存 | `ldconfig -p \| grep libspdk_event` | `ldconfig` |
| 容器 cpuset | `cat /sys/fs/cgroup/cpuset.cpus.effective`（要 0-383） | host: 放开 docker 父级+容器层 cpuset 到 0-383 |
| SPDK 残留 | `ls /var/tmp/spdk* /dev/hugepages/umbp_spdk_proxy` | `rm -f /var/tmp/spdk_cpu_lock_* /var/tmp/spdk.sock* /dev/hugepages/umbp_spdk_proxy; rm -rf /var/run/dpdk/spdk_pid*` |

## 常见报错 → 根因 → 修法

- `EAL: FATAL: Cannot get hugepage information` → /dev/hugepages 没挂 或 nr_hugepages=0 → 挂载 + 设 nr。
- `EAL: No free 2048 kB hugepages reported on node N` → 该 node hugepage 被占光/不足（proxy ring 要 32GB）→ 加大 nr，清 `/dev/hugepages/umbp_spdk_proxy` 残留。
- `spdk_app_start returned rc=1` + `unclaim_cpu_cores: Failed to unlink lock fd for core 0` → 真因看上面几行（多半是上面的 hugepage 错），unclaim 那行是回滚噪声。
- `claim_cpu_cores ... g_core_locks` segfault → `MAX_CPU_CORES=128` < 机器核数；改 512 重编 `libspdk_event.so`（注意要 `--with-shared` 才会重链 .so）。
- `host memory should be larger than device memory` → `HICACHE_SIZE` < 设备 KV 池；用 banff 验证值 `HICACHE_SIZE=128 + --max-total-tokens 600000 + --mem-fraction-static 0.8`。
- `not eligible; choose between [0..127]` → 容器 cpuset 被裁到 0-127 → 放开到 0-383。
- `memory capacity unbalanced ... local_gpu_memory=~1.4GB` → 8 卡被别的任务占满（共享机争用）→ 等 GPU 空闲，别乱杀别人任务。
- `symmetric_memory.cpp hip failed / Cuda failure invalid argument`（MoE）→ 用 mori main（含 #344 skip RDMA MR when no peers）。
- **`Memory access fault by GPU node-N on address 0x7f...` + dmesg `PERMISSION_FAULTS:0x5 / VM_L2_PROTECTION_FAULT / UTCL2 client TCP`（prefill 前向时崩）** → hicache 默认 `--hicache-io-backend kernel` 让 GPU kernel 直接读写 host KV 缓冲，skyriver07 上 `hipHostRegister` 对大缓冲不生效 → GPU 越权访问。**修法：加 `--hicache-io-backend direct`（走 hipMemcpyAsync，不让 GPU 解引用 host 指针）。**
  - bisect 证据：case1(HBM_Only,无 hicache) PASSED；case2(HBM_DRAM,有 hicache 无 UMBP) 也 fault；case3 同样 → 与 mori MoE / UMBP / SPDK 全无关，纯 hicache transfer 问题。
  - 注意：`kernel` backend 在 banff 能用、skyriver07 不能用 → 机器/ROCm 差异，**skyriver07 必须 direct**。

## 验证过的跑通命令（skyriver07 上 case3 PASSED，reuse，不要瞎改参数）

2026-06-01 在 skyriver07 用下面这条 **case3:HBM_DRAM_SSD PASSED (50s)**，
133 次 BatchPut，bandwidth min=1.98 / p50=3.18 / max=3.66 / **avg=2.92 GiB/s**。
关键是 `--hicache-io-backend direct`（无它必 GPU fault）+ `UMBP_SPDK_IO_WORKERS=1`。

```
EXTRA_SERVER_ARGS="--mem-fraction-static 0.8 --max-total-tokens 600000 --hicache-io-backend direct" \
HICACHE_SIZE=128 SGLANG_MORI_NUM_MAX_DISPATCH_TOKENS_PER_RANK=16384 \
MODEL_PATH=/mnt/nvme1/data/DeepSeekV3.1 UMBP_SSD_BACKEND=spdk_proxy \
UMBP_SPDK_NVME_PCI=0000:88:00.0 \
UMBP_SPDK_PROXY_BIN=/opt/venv/lib/python3.10/site-packages/mori/spdk_proxy \
UMBP_SPDK_IO_WORKERS=1 \
UMBP_LOG_LEVEL=0 CASES_OVERRIDE=case3:HBM_DRAM_SSD DUMMY_FORWARD=true \
DO_CHECKOUT=false tools/iobw/run_umbp_iobw_test.sh
```

- `BatchGet=0` 是正常的：fresh cache 全写无读，要测读带宽需触发 prefix 命中（多轮重复 prompt）。
- soft lockup 在 skyriver07 上即使 case1（无 SPDK）也会涨，是该机 GPU/kfd 层噪声，**不致命、不阻断 run**，别再归因到 SPDK io_workers。

## 测 DRAM→HBM 逐层 load 带宽（host pool L2 → device pool L1）

- **路径（已 trace 确认，唯一）**：`cache_controller.py:start_loading` 逐层调
  `mem_pool_host.load_to_device_per_layer(...)`。**没有别的 host→device load 旁路**；
  `*_all_layer` 全是 `backup_from_device_all_layer`（写回 device→host），别搞混。
- **micro bench（`bench_umbp_micro` / `run_umbp_iobw_test.sh` 直调 UMBPStore）不过 controller，
  永远测不到这个**——必须跑全栈 server（`--enable-hierarchical-cache`，即 case2/case3）。
- 埋点：`MLATokenToKVPoolHost.load_to_device_per_layer`，开关 `SGLANG_HICACHE_LOAD_TIMING=1`。
  首次进入打 `[HICACHE-LOAD] ENTER ...`（stderr，证明被调到），之后每层打
  `[HICACHE-LOAD] layer=.. tokens=.. GiB/s=..`。**stderr 直出，不受 log level 过滤**。
- 触发条件：要有 device(HBM) 驱逐 + prefix 复用 → 多轮重复 prompt（`READ_REPLAY=true` 最稳）。
- **跑通命令（在上面验证过的 case3 命令基础上加两个开关，case2 也行但同样要 `--hicache-io-backend direct`）**：

```
SGLANG_HICACHE_LOAD_TIMING=1 READ_REPLAY=true \
EXTRA_SERVER_ARGS="--mem-fraction-static 0.8 --max-total-tokens 600000 --hicache-io-backend direct" \
HICACHE_SIZE=128 SGLANG_MORI_NUM_MAX_DISPATCH_TOKENS_PER_RANK=16384 \
MODEL_PATH=/mnt/nvme1/data/DeepSeekV3.1 UMBP_SSD_BACKEND=spdk_proxy \
UMBP_SPDK_NVME_PCI=0000:88:00.0 \
UMBP_SPDK_PROXY_BIN=/opt/venv/lib/python3.10/site-packages/mori/spdk_proxy \
UMBP_SPDK_IO_WORKERS=1 UMBP_LOG_LEVEL=0 \
CASES_OVERRIDE=case3:HBM_DRAM_SSD DUMMY_FORWARD=true \
DO_CHECKOUT=false tools/iobw/run_umbp_iobw_test.sh
```

- io_backend=`direct` 时 load 走 `transfer_kv_direct` / `transfer_kv_per_layer_direct_pf_lf`
  分支（埋点已覆盖）；`kernel` 在 skyriver07 会 GPU fault，**必须 direct**。

### ⚠️ 触发 host→device load 的必要条件（否则埋点永不打）

`load_to_device_per_layer` 只在 `req.host_hit_length > 0` 时触发
（`schedule_policy.py:792` → `init_load_back` → `cache_controller.load()` + `start_loading()`）。
即**前缀必须先从 L1(HBM) 驱逐到 L2(host DRAM)，再次复用命中 L2 才会 load 回 device**。
- `--max-total-tokens` 太大 → L1 装得下全部前缀、永不驱逐 → `host_hit_length=0` → load 从不触发。
- **必须把 L1 压小**逼驱逐：`--max-total-tokens 60000` + 多客户端多轮复用（`NUM_CLIENTS=16 NUM_ROUNDS=20`）。
- case3 的 READ_REPLAY 走的是 storage→host **异步 prefetch**，dummy 重算太快、请求在 prefetch 落地前就完事 → 同样测不到 host→device load。**测 DRAM→HBM 要用 case2 + 小 L1，不是 case3 replay。**
- `dummy_forward` 不影响 load（load_back 在 scheduler 里、forward 之前，只看 host_hit_length），可继续用 dummy 省 compute。

### 实测结果（skyriver07，06-03，DeepSeekV3.1，case2，direct，page_first_direct）

| 测法 | 带宽 | 说明 |
|---|---|---|
| 逐层（`SGLANG_HICACHE_LOAD_TIMING=1`，每层 sync） | p50 5.71 / avg 5.53 GiB/s，大 token 封顶 ~5.8 | n=3355，小 token(64) 仅 1.6（sync 主导） |
| **整次 61 层（`SGLANG_HICACHE_LOAD_AGG=1`，单 sync）** | **avg 5.96 / p50 5.95 / max 6.15 GiB/s，≥2048token 稳 ~6.0** | n=92，**这是干净的有效带宽** |

- **真实 DRAM→HBM hicache load ≈ 6.0 GiB/s（≈6.4 GB/s）**，且**与 transfer 大小无关**（2048~9344 token 都 ~6.0）。
- 远低于裸连续 pinned 拷的 ~57 GB/s → 瓶颈是**按 token 散列 gather**（`transfer_kv_per_layer_direct_pf_lf`，hipMemcpyAsync），不是 host↔GPU 链路。
- 两种测法一致（5.8 vs 6.0）→ 逐层 sync 污染很小，结论稳。

### 核心结论：SSD 能否替代 DRAM 作 KV load 源

| 阶段 | 带宽 |
|---|---|
| DRAM 路径：host→device gather（本测） | **~6.0 GiB/s** |
| SSD 路径：SSD→host 冷读（RAID0，bench_umbp_micro 64GB 工作集） | 8.72 GiB/s |
| + 再 host→device gather（公共步骤） | ~6.0 GiB/s |

- **host→device gather(~6.0) 才是整条 load 链的绑定瓶颈，而它比 SSD 冷读(8.72) 还慢。**
- ⇒ SSD 读带宽(8.72) > gather(6.0)，**SSD 喂得过来**；DRAM 唯一优势（省掉 SSD→host）在 gather 成为瓶颈后**不再有用**。
- ⇒ **SSD 可替代 DRAM 作 load 源，带宽损失极小**——只要 SSD→host 与 host→device 能 pipeline 重叠（hicache prefetch 本就异步），SSD 路径有效带宽 ≈ min(8.72, 6.0) = 6.0，与 DRAM 路径持平。
- 前提/边界：① 测的是 `direct`+`page_first_direct` 散列 gather（skyriver07 必须 direct）；② SSD 冷读须达 RAID0 8.72（小工作集 cache 数不算）；③ 串行不重叠时 SSD 路径降到 ~3.55 GiB/s，需确认 prefetch 与 load 真重叠。

### prefetch 与 load 重叠性（06-03 代码 trace 确认，第③点已钉）

调用链（scheduler.py）：
- `_add_request_to_queue:1748` → `_prefetch_kvcache:1716` → `tree_cache.prefetch_from_storage`：
  **请求一入 `waiting_queue` 就立刻异步发起 prefetch**（投进 `prefetch_queue`，由 `cache_controller.prefetch_thread_func` 独立线程做 storage→host，与主调度/load 解耦）。
- staging 时 `get_new_batch_prefill:2158` `check_prefetch_progress(req.rid)`：**prefetch 未完成 → `continue` 跳过该请求、先调度别的**；完成后才 `add_one_req` → `init_load_back` → `start_loading`（host→device）。
- ⇒ **prefetch(SSD→host) 在入队时提前跑，与其它在飞请求的 load(host→device)/compute 跨请求 pipeline 重叠**——重叠是设计内的，不是串行。
- **结论钉死**：批量服务（多并发请求，真实场景）下 pipeline 填满 → SSD 路径有效带宽 = min(SSD冷读 8.72, gather 6.0) = **6.0 GiB/s ≈ DRAM 路径**。**SSD 可替代 DRAM 作 KV load 源，带宽无损**。
- 仅在并发不足以掩盖 prefetch 延迟时（单请求/冷启动）退化为串行 ~3.55；稳态批服务不受影响。
- 仍可加的终极实测（可选）：case2(DRAM) vs case3(SSD 冷, 工作集>32GB proxy ring) 端到端吞吐对比，若相等即闭环。

## 问题与成果记录（2026-06-01 skyriver07）

### 成果（目标达成）
- **skyriver07 全栈 case3:HBM_DRAM_SSD PASSED**（DP8 + mori MoE + UMBP + spdk_proxy + SSD）。
- IO 写带宽 telemetry 真实采到：
  - 06-01 首次：133 次 BatchPut，avg 2.92 GiB/s。
  - **06-02 重启后干净复现：138 次 BatchPut，avg 3.445 / p50 3.475 / p95 3.671 / max 3.675 GiB/s**（14.36 GB，典型 batch 137.25 MiB，per-call 中位 36.5ms）。
- **06-02 BatchGet 读路径打通**（`READ_REPLAY=true` 新模式）：能触发 SSD 读、telemetry 有数。
  - 机制：写阶段 write_through 落 SSD → `/flush_cache`（`HiRadixCache.reset()` 只清 HBM/DRAM，**storage 存活且 prefetch 可达**）→ 重放同样 prompts → 命中 storage → BatchGet。
  - 开关：`READ_REPLAY=true`（bench_multiturn `--read-replay`）。
  - **⚠️ 所有读带宽数字到目前为止都不可信（cache 命中）**。见下方「读带宽」专节。
- 8 个 DP rank 的 `[UMBPStore][IOBW]` 日志 + JSONL 旁路文件全部产出。
- **JSONL 旁路确认有效**：06-02 diagnose 自动重算出 8 files / 138 call records / avg 3.445。
  （06-01 那次报"0 records"是采集时机问题——手动跑 summarizer 时 runner 还没把 JSONL 移进结果目录，**不是 bug**。）
- banff 5/29 也 PASSED 过（avg 1.405 GiB/s，不同机器/参数）。

### 重启后恢复流程（06-02 验证有效，照此顺序）
1. host: rebind `0000:88:00.0`（=nvme2/nvme2n1，7T 未挂载空盘，**先确认未挂载再 rebind**）：
   `echo 0000:88:00.0 > /sys/bus/pci/drivers/nvme/unbind; echo vfio-pci > .../driver_override; echo 0000:88:00.0 > /sys/bus/pci/drivers/vfio-pci/bind`
2. host: hugepage `echo 24576 > node{0,1}/.../nr_hugepages`（共 49152，全 free）。
3. 读 iommu group：`basename $(readlink .../0000:88:00.0/iommu_group)` = **110**，`cat /sys/class/vfio/110/dev` = **239:0**。
4. 容器内：`mknod -m666 /dev/vfio/110 c 239 0`；`mkdir -p /dev/hugepages; mount -t hugetlbfs nodev /dev/hugepages`。
5. 清残留：`rm -f /var/tmp/spdk_cpu_lock_* /var/tmp/spdk.sock* /dev/hugepages/umbp_spdk_proxy`。
6. 复检全绿后直接跑下面"验证过的命令"。

### 排障时间线（每一步都 bisect 验证，非臆断）
1. `OSError: Can't load config /nfs/data/DeepSeek-V3` → `/nfs/data` 重启消失 → 改 `MODEL_PATH=/mnt/nvme1/data/DeepSeekV3.1`。
2. proxy attach 崩 → PCI 用错（`0000:50:00.0` 是 banff 的）→ skyriver07 用 `0000:88:00.0`。
3. `EOFError` scheduler init → 用对 PCI + verified config 后消失。
4. **`Memory access fault by GPU`（prefill 前向）→ 真正的大坑**：bisect 出 case1(无 hicache)PASSED、case2/3(有 hicache)fault → 根因是 `--hicache-io-backend kernel` 默认值，skyriver07 上 GPU 直访 host 缓冲越权 → **改 `--hicache-io-backend direct` 修复**。
5. 全部叠加后 PASSED。

### 仍未决：soft lockup（不阻塞，但要查）
- 现象：`watchdog: BUG: soft lockup - CPU#N stuck 22s [kworker/N]`，重负载下大面积，跨多核。
- **重要修正**：原以为是 `spdk_proxy` busy-poll（`spdk_ssd_tier.cpp:544` 无 `CPU_PAUSE` 的 drain 忙等 + 不绑核 worker 线程）独占造成。但 **case1（无 SPDK/UMBP）跑通时 soft lockup 计数照样涨**（78→88），证明**不只是 SPDK** —— 这台机器的 GPU/amdgpu/kfd 活动（含 faulted context 的 `kfd_process_wq_release` 清理）本身就触发。
- `UMBP_SPDK_IO_WORKERS=1` 仍建议保留（减少不绑核忙等线程，降低 SPDK 那部分贡献），但它**不是 soft lockup 的完整解**。
- 现状：soft lockup **不致命、不阻断 run**（case3 PASSED 时也在涨）。属内核/驱动层（kernel 5.4.241 + ROCm）问题。
- 待深挖方向：(a) `spdk_ssd_tier.cpp:544` drain 加 `CPU_PAUSE` + worker 绑核（降 SPDK 贡献）；(b) 查 amdgpu/kfd 在大 free/IPC 时的 kworker 卡顿；(c) 对比 banff（同负载不触发？）定位是否纯 skyriver07 内核版本问题。

### 带宽实测结论（skyriver07，06-02，apples-to-apples，bench_umbp_micro --profile large io_workers=4）

硬件（已用 sysfs 硬数据确认）：盘 = Samsung PM9A3 级 [144d:a80a]，7.68TB，**PCIe Gen4 x4（current_link_speed=16 GT/s, width=4）**。
PCIe Gen4 x4 物理上限 = 16×4×(128/130)/8 ≈ **7.88 GB/s/盘**（链路硬顶，NAND 再快也过不去）。RAID0=2 盘聚合上限 ≈ 15.76 GB/s。
**任何单盘读 > 7.88 GB/s 一律是 cache，没有例外。**

**可信结论（64 GiB 工作集冷读，bench_umbp_micro --value-size 4MiB --num-keys 16384 --batch-size 16 io_workers=4，全部过 PCIe sanity check）：**
| | 单盘(88) | RAID0(88+c3) | 倍数 |
|---|---|---|---|
| SSD WriteBatch | 3.25 GiB/s (3.49 GB/s) | 5.21 GiB/s (5.59 GB/s) | **1.60×** |
| SSD ReadBatch | 4.91 GiB/s (5.27 GB/s) | 8.72 GiB/s (9.36 GB/s) | **1.78×** |

- **RAID0 读写都真有效**（写 1.60×、读 1.78×，真双盘条带）。
- 铁证：RAID0 读 9.36 GB/s **> 单盘 PCIe 上限 7.88 GB/s**，单盘物理不可能 → 确实条带到两块盘。
- 所有数 < 双盘聚合上限 15.76 GB/s ✅。

**小工作集（几百 MB，如 --profile large 默认）读出来的 14~16 GiB/s 全是 SSD 控制器 DRAM cache，作废。** 测真带宽必须工作集 >> 控制器 DRAM（≥ 64GB 稳妥）冷读。proxy 路径之前的 16 GiB/s 同理（proxy ring cache）。

**RAW 裸盘上限（bdevperf 直打 bdev，1MiB io，qd256，绕开 tier pipeline）：**
| | 单盘(88) | RAID0(88+c3) | 倍数 |
|---|---|---|---|
| RAW 读 | 6.75 GB/s | 13.56 GB/s | **2.01×** |
| RAW 写 | 4.17 GB/s | 8.30 GB/s | **1.99×** |

- 验证：单盘 RAW 读 6.75 ≈ PM9A3 标称 ~6.8、写 4.17 ≈ 标称 ~4.1 GB/s，吻合 → 可信。RAID0 精确 2× 条带。
- **层级损耗**：RAW → SpdkSsdTier batch（读 9.36/写 5.59 GB/s）损 ~20-33%（memcpy+submit+drain）→ proxy 路径再往下。
- bdevperf：`bdevperf -c <json> -s 4096 -m 0xFF -q 256 -o 1048576 -t 15 -w read|write -T Raid0|NVMe0n1`；JSON 用 bdev_nvme_attach_controller(×2) + bdev_raid_create(raid0, strip 128KB)。

### 全栈 proxy 写瓶颈（06-02 定位，代码+历史双证）

全栈 case3 写带宽 **3.43 GiB/s**，io_workers 1→4 **无变化**，RAID0 也吃不进去（≈单盘 tier 写 3.49）。
- **根因**：`spdk_proxy_tier.cpp:487` client 端把 KV 灌进共享 ring 是**单线程 memcpy**（pinned host→hugetlb shm，~3.43 GB/s）。proxy 被 `bytes_ready` 门控（`spdk_proxy_daemon.cpp:796` / `spdk_ssd_tier.cpp:669`），流水线速率 = min(单线程 memcpy, SSD 写)。单盘时两者相当；RAID0(5.59~8.3) 快过 memcpy → **memcpy 成瓶颈**，proxy 被饿着，故 RAID0/io_workers 无效。
- **历史**：`git log -S` 确认 `bytes_ready`/`kCopyChunk`/`BatchWriteStreaming` 均 **#213(2026-03-23 首版 SPDK 后端)引入**；#223(多租户重构) 沿用未改。→ **原始设计，非 regression**；#213 按单盘设计，没考虑 RAID0 这种快后端。
- **修法（已改，opt-in）**：`spdk_proxy_tier.cpp` 加 `UMBP_SPDK_PROXY_COPY_THREADS`（默认 1=原行为零风险），>1 时并行 memcpy 灌 ring、`bytes_ready` 末尾一次性发布。需重编 mori 验证写带宽能否从 3.43 升向 ~5.59。

### 其它遗留
- ~~`BatchGet=0` 测不到读带宽~~ → **已解决**：`READ_REPLAY=true`（写→flush→重放）可触发 SSD 读。注意小工作集下读数是 cache（见上）。
- ~~`summarize_iobw_jsonl.py` 报 0 records~~ → **已澄清非 bug**（采集时机问题，06-02 自动重算 138 records 正常）。
- ~~`diagnose_iobw_logs.sh` 多行计数语法错~~ → **已修**（`grep -c || echo 0` 会产生 `0\n0`，去掉 `|| echo 0` 改用 `${var:-0}`）。
