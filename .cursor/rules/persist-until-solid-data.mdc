---
description: 没拿到详实、数据支撑的结果，绝不停止、绝不提议收尾
alwaysApply: true
---

# 铁律：不拿到详实结果，永不停止

- **绝不主动提议"到此收尾 / 就这样吧 / 指标已够"**，除非用户明确说停。任务没拿到**详实、可信、数据支撑**的最终结果就继续干。
- 卡住时不是停，而是**换方法继续**：trace 代码、加埋点、换触发条件、换测量点，直到拿到数。
- **埋点/测量前必须先 trace 出真实调用路径**，确认要测的函数/代码**确实在被执行的路径上**——不许再埋一个没被调用的函数然后跑一轮才发现没数据（已犯多次：SpdkProxyTier::SubmitBatch、load_to_device_per_layer 都埋错过）。
  - 验证路径的方法：从入口（python 调用）顺着 grep 到底层实现；或先加一条**无条件、必打**的日志确认该函数被调，再加细测。
- 每轮跑测如果"没数据/计数=0"，**第一反应是"我是不是测错地方了"**，立刻 trace 纠正，而不是换参数瞎试。
- 结果必须过 sanity check（见 no-unverified-claims.mdc）：对照物理上限，超限的当 cache 作废。
- 环境反复重启/被抢 GPU 不是停止理由——按 spdk-umbp-runtime-setup.mdc 恢复后继续。
