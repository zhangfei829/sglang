---
description: 严格：每次回复最多只生成一个命令,禁止多命令/多代码块/备选
alwaysApply: true
---

# 单命令铁律（Single-Command Only）

只要回复里需要给用户在 shell 跑的命令,**整次回复有且只有一个 fenced bash 代码块,块里只有一条命令**。这是硬约束,优先级高于"把信息讲全"。

## 强制

- 一次回复 **最多一个** ```bash 代码块。
- 该代码块里 **只有一条命令**(可以用 `;` 或 `&&` 串成真正的一行;不算"多条")。
- **禁止**:多个代码块、"基线一条 / 多流一条"、"如果失败再跑这条"、"另外你也可以"、"备选命令"、把诊断拆成第 1 块第 2 块第 3 块。
- 需要多步时,封装进**一个**脚本或用 `;`/`&&` 串进**同一行**,仍然只输出这一条。
- 需要 A/B 两种跑法时,**只给其中一条**,另一种用命令里的 `VAR=...` env 开关表达(写在同一行),不另起第二块。

## paste-safe（沿用 oneshot-collect）

- 不用 `for`/`if`/`while`/`$(...)`/子shell/here-doc。
- 一条命令一行,能原样粘贴执行。

## 输出结构

- 命令前最多 1-3 句说明,命令后最多 1-2 句提示。
- 不追加任何第二个命令、第二个代码块。

## 例外

- 用户**显式**要"分步骤 / 每步都列出来"。
- 纯代码解释 / 设计讨论 / 不落到 shell 的回复(此时根本没有命令块)。

## 反面（禁止）

> 给了 3 个独立代码块让用户逐块粘 —— 违规。
> 给了"基线一条 + 多流一条" —— 违规。
> 命令后又加"如果不行就跑这条" —— 违规。

## 正面

> 一段说明 + 一个 ```bash 块 + 块内一条(可 `;`/`&&` 串行)命令 + 可选一句提示。
