---
description: 没有数据支撑绝不猜测；性能数字必须对照硬件规格做 sanity check 再报
alwaysApply: true
---

# 铁律：没有数据支撑，绝不猜测、绝不下结论

违反过的真实教训（必须避免重演）：
- 把 proxy ring cache 命中的 16 GiB/s 当成真盘读带宽报出去。
- 拿 **banff**（`0000:50:00.0`）的单盘 13.6 GB/s 当 **skyriver07**（nvme2/nvme3）的基线去比 RAID0 —— 不同机器、不同硬件，比较无效。
- 报 14 GiB/s 单盘读，却没对照「单盘 NVMe datasheet 顺序读上限 ~8 GB/s」做 sanity check —— 超规格 = 必是 cache。

## 报任何性能数字前，强制三问

1. **来源**：这个数是我**这次、在当前这台机器/这块盘**实测的吗？还是从规则文件/历史/别的机器搬来的？
   - 搬来的、别的机器的、来路不明的 → **不能当当前环境基线**，必须现场重测，并注明 provenance（机器名 + PCI/设备 + 命令）。
2. **物理上限 sanity check**：这个数有没有超过硬件规格？
   - 读数 > 单盘 datasheet 顺序读 → **必是 cache（控制器 DRAM / OS page cache / 应用 buffer / 刚写完的 ring）**，不是介质带宽，**标注为 cache，不得当真盘带宽报**。
   - 写数应 < 单盘顺序写规格；RAID0 = N 盘规格之和。
   - 先查/确认硬件规格（`lspci -vnn`、datasheet、容量），再判数据真假。
3. **cache vs 介质**：测真实介质带宽，工作集必须 **远大于所有 cache**（SSD 控制器 DRAM 通常几 GB；proxy ring 32GB）。小工作集 + 刚写完就读 = 必中 cache。

## 行为要求

- 不确定就直说「不确定，需要实测」，然后去测——**不要编、不要拿旧数充当**。
- 结论必须有当次实测数据支撑；没有就不下结论。
- 发现自己之前报的数可能是 cache/搬运/超规格，**立刻主动纠正并标注作废**，不藏着。
- 对照规格发现矛盾（如读 > 规格上限），优先怀疑「测量方法/cache」，不要强行解释成「真带宽」。

## 适用面

不止带宽——任何「数字 / 基线 / 结论」都适用：延迟、容量、核数、命中率、版本号、路径……
凡是要写进汇报或规则的事实，先确认来源 + 合理性，再写。
